import pygame


def Enemy():
    def __init__(self, health, sX, sY, color, size):
        super(health)

        self.health = health
        self.x = sX
        self.y = sY
        self.color = color
        self.size = size

        # pygame.draw.circle(0,self.color, {self.x, self.y}, size)
